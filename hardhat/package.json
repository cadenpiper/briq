{
  "name": "briq-core",
  "version": "1.0.0",
  "description": "Smart contracts for Briq DeFi yield optimization protocol",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "test:coverage": "npx hardhat coverage",
    "compile": "npx hardhat compile",
    "clean": "npx hardhat clean",
    "node:arbitrum": "npx hardhat node --network forkedArbitrum",
    "node:ethereum": "npx hardhat node --network forkedEthereum --port 8546",
    "deploy": "npx hardhat run scripts/forking/deploy.ts",
    "configure": "npx hardhat run scripts/forking/configure.ts",
    "fund": "npx hardhat run scripts/forking/fundAccount.ts",
    "fund-rupert": "npx hardhat run scripts/forking/fundRupert.ts",
    "set-rupert": "npx hardhat run scripts/forking/setRupert.ts",
    "balance": "npx hardhat run scripts/forking/checkBalance.ts",
    "setup:arbitrum": "npm run deploy -- --network localArbitrum && npm run configure -- --network localArbitrum && npm run fund -- --network localArbitrum && npm run fund-rupert -- --network localArbitrum && npm run set-rupert -- --network localArbitrum && npm run balance -- --network localArbitrum",
    "setup:ethereum": "npm run deploy -- --network localEthereum && npm run configure -- --network localEthereum && npm run fund -- --network localEthereum && npm run fund-rupert -- --network localEthereum && npm run set-rupert -- --network localEthereum && npm run balance -- --network localEthereum"
  },
  "keywords": [
    "defi",
    "yield",
    "optimization",
    "ethereum",
    "arbitrum"
  ],
  "author": "Briq Protocol",
  "license": "UNLICENSED",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^3.0.5",
    "@nomicfoundation/hardhat-network-helpers": "^3.0.3",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.1",
    "@types/node": "^22.19.1",
    "forge-std": "github:foundry-rs/forge-std#v1.9.7",
    "hardhat": "^3.0.14",
    "typescript": "~5.8.0",
    "viem": "^2.39.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "@chainlink/contracts": "^1.5.0",
    "@pythnetwork/pyth-sdk-solidity": "^4.2.0",
    "@aave/core-v3": "^1.19.3",
    "@nomicfoundation/hardhat-keystore": "^3.0.3"
  }
}
